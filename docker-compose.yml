services:
  detector:
    build: .
    container_name: fake-news-detector
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped